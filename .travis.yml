language: java
jdk:
  - openjdk7
  - oraclejdk7  
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'DevWars' ]; then cd .. && eval "mv $currentfolder DevWars" && cd DevWars; fi
  - npm install -g bower grunt-cli grunt
  - bower install && npm install && grunt
